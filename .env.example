# このファイルは「見本」です。中身をコピーして .env を作り、実際のキーを書きます。
# .env は .gitignore 済みなのでGitHubには上がりません（安全）。

# Anthropic APIキー（要約に必須）。console.anthropic.com で取得。
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# NCBI APIキー（任意）。あれば 3→10 req/秒に緩和される。無くても動く。
# NCBI_API_KEY=
